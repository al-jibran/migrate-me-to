{
	"name": "e2e",
	"description": "A sub-module to run cypress tests. Otherwise cypress pollutes the global namespace of the parent module and conflicts with jests.",
	"scripts": {
		"cypress:open": "cypress open",
		"cypress:run": "cypress run"
	},
	"license": "ISC",
	"devDependencies": {
		"@testing-library/cypress": "^8.0.2",
		"cypress": "^9.4.1",
		"typescript": "^4.5.5"
	},
	"dependencies": {
		"cypress-real-events": "^1.7.0"
	}
}
