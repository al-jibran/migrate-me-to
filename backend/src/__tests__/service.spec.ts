// import axios from 'axios';

// const baseUrl = 'http://localhost:4100';

describe('Service', () => {
	it('returns a link to login to the service', async () => {
		// const { data } = await axios.get(`${baseUrl}/login/twitter`);
		expect(2).toBe(2);
	});
});
